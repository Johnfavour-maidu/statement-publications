{
  "name": "statement-publications",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "clean": "turbo clean",
    "db:generate": "pnpm --filter @statement/database db:generate",
    "db:push": "pnpm --filter @statement/database db:push",
    "db:migrate": "pnpm --filter @statement/database db:migrate",
    "db:studio": "pnpm --filter @statement/database db:studio"
  },
  "devDependencies": {
    "next": "16.2.7",
    "turbo": "^2.5.0"
  },
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": ">=18"
  }
}
